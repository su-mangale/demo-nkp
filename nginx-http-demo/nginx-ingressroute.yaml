apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nginx-ingressroute
spec:
  entryPoints:
  - websecure
  routes:
  - match: PathPrefix(`/nginx`)
    kind: Rule
    middlewares:
    - name: strip-nginx
    services:
    - name: nginx-service
      port: 80
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: strip-nginx
spec:
  stripPrefix:
    prefixes:
    - /nginx

